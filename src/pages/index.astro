---
import Layout from '@layouts/Page.astro'
import MultiNetwork from '@components/MultiNetwork'
import { buildDictionaryFromNetwork } from '@build/cedict'
import ren from '~/public/ctext/仁_pmi.json'
import { buildDictionaryFromNetworks } from '@build/cedict'

const renDict = await buildDictionaryFromNetworks(ren)
---

<Layout>
  <main class="mx-auto w-full px-4 py-8">
    <div class="items-center justify-center xl:flex">
      <div class="xl:w-1/2">
        <MultiNetwork
          client:load
          data="/ctext/仁_pmi.json"
          sourceAlign="left"
          dictionary={renDict}
        />
      </div>
      <div class="mt-8 xl:mt-0 xl:w-1/2">
        <MultiNetwork
          client:load
          data="/sep/benevolence_pmi.json"
          sourceAlign="right"
        />
      </div>
    </div>
  </main>
</Layout>
