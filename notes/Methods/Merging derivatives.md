Once embeddings were calculated, a second  pre-processing step ran on the SEP corpus, which merged tokens with similar embeddings and a common derivational root. Whether two tokens shared a common root was determined by whether they 

...the *Open English Wordnet* (2025).

Once debiasing was accomplished these started clumping together in the similarity graph + embedding space. This was a sign that things were working. But it also visually cluttered both graphs.

See [[derivational-variant-merging]]