"""Word segmentation and (optional) pos tagging using XunziALLM.

Library modules (seg, segpos, utils) + few-shot data (seg_data.json); the
runnable entrypoint is ``cli/segment.py``. Run with ``scripts/`` as the working
directory so sibling packages (``config``, ``corpus``) resolve on ``sys.path``.
"""
