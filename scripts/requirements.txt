# Direct dependencies only. Transitive deps are resolved by pip.

# --- NLP / ML ---
numpy>=2.4.4,<3                   # pin: scipy/sklearn need >=2.x; a 1.x downgrade breaks sklearn
scikit-learn>=1.8.0,<2
spacy>=3.8.14,<4                  # English pipeline (nlp/english.py); also used by suparkanbun
spacy-html-tokenizer>=0.1.3,<0.2
suparkanbun>=1.7.7,<2             # classical-Chinese seg + UD POS (spaCy/esupar); pulls transformers
torch==2.9.1+rocm6.3              # must be installed separately, see init-env.sh

# --- scraping / data / output ---
beautifulsoup4>=4.14.3,<5
requests>=2.33.1,<3
requests-cache>=1.3.1,<2
networkx>=3.6.1,<4
python-slugify>=8.0.4,<9
tqdm>=4.67.3,<5

# --- linting ---
autopep8>=2.3.2,<3
pyright>=1.1.409,<2
