# Direct dependencies only. Transitive deps are resolved by pip.

# --- NLP / ML ---
numpy==2.4.4                 # pin: cltk/scipy need >=2.x; a 1.x downgrade breaks sklearn
scikit-learn==1.8.0
transformers==5.13.0
spacy==3.8.14
spacy-html-tokenizer==0.1.3
cltk==2.5.1                  # pulls in stanza + the classical-Chinese pipeline
stanza==1.11.1
openai==2.45.0
torch==2.9.1+rocm6.3         # must be installed separately, see init-env.sh

# --- scraping / data / output ---
beautifulsoup4==4.14.3
requests==2.33.1
requests-cache==1.3.1
networkx==3.6.1
python-slugify==8.0.4
tqdm==4.67.3

# --- linting ---
autopep8==2.3.2
pyright==1.1.409
